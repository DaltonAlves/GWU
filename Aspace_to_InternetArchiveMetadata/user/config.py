USER = '' #put aspace username here
PASSWORD = '' #put aspace password here
HOST = '' #put API host URL here. Make sure you don't have a slash ('/') at the end of the URL.
PUI = 'https://searcharchives.library.gwu.edu/repositories/2/' #put the public user interface URL here. It should include the specific repository and end with a / (ex. test.edu/repositories/2/)

config = {
    'USER': USER,
    'PASSWORD': PASSWORD,
    'HOST': HOST,
    'PUI': PUI
}